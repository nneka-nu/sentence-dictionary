doctype
html
  head
    meta(charset="utf-8")
    title Sentence Dictionary
    link(rel="stylesheet" href="style.css")
  body
    div(id="root") !{html}
    script.
      window.__INITIAL_STATE__ = !{JSON.stringify(initialState).replace(/</g, '&lt;').replace(/>/g, '&gt;')}
    script(src="vendors.js")
    script(src="bundle.js")